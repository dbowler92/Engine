The engine will be output as a .lib. This project will be a sandbox that I will be using to implement 
engine features. Same idea really as GameProject, but much less fun from the users point of view!